const PX = 'px'
const EM = 'EM'
const REM = 'REM'
const PERCENT = '%'

export const px = (n) => n + PX
export const em = (n) => n + EM
export const rem = (n) => n + REM
export const percent = (n) => n + PERCENT
