import styled from 'styled-components'

import themeProps from '../../theme/themeProps'

const CardSubtitleText = styled.h4`
  font-size: 0.875rem;
  color: ${themeProps.textSecondaryOnLight};
  margin: 0;
`

export default CardSubtitleText
