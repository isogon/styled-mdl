import styled from 'styled-components'

const CardTitleText = styled.h1`
  align-self: flex-end;
  color: inherit;
  display: block;
  display: flex;
  font-size: 1.5rem;
  font-weight: 300;
  line-height: normal;
  overflow: hidden;
  margin: 0;
`

export default CardTitleText
