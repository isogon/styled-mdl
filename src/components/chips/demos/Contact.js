import React from 'react'
import { Chip } from '../../../'

/* eslint-disable no-alert */
export default () => (
  <Chip contact={{ color: 'teal|600', textColor: 'white', text: 'A' }}>
    Contact Chip
  </Chip>
)
