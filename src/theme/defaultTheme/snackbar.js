import { gridTabletBreakpoint } from './grid';
import { colorAccent } from './colors';

// Hard coded since the color is not present in any ..get(snackbarBackgroundColor): #323232,
export const snackbarTabletBreakpoint = gridTabletBreakpoint;
export const snackbarActionColor = `rgb(${colorAccent})`;
