import React from 'react';
import { Checkbox } from 'material-components';

const demo = () => <Checkbox label="Checkbox" />;

const caption = 'Checkbox Off';
const code = '<Checkbox label="Checkbox" />';

export default { demo, caption, code };
