import React from 'react';
import { Switch } from 'material-components';

const demo = () => <Switch defaultChecked />;

const caption = 'Switch On';
const code = '<Switch defaultChecked />';

export default { demo, caption, code };
