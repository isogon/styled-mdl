import React from 'react';
import { Switch } from 'material-components';

const demo = () => <Switch />;

const caption = 'Switch Off';
const code = '<Switch />';

export default { demo, caption, code };
