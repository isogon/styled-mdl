import React from 'react';
import { Spinner } from 'material-components';

const demo = () => <Spinner active singleColor />;

const caption = 'Single Color';
const code = '<Spinner active singleColor/>';

export default { demo, caption, code };
